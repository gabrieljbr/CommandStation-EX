#ifndef FLASHINGSIGNALS_H
#define FLASHINGSIGNALS_H

#include "SigState.h"  // Include the header that defines SignalState

extern SignalState flashingSignals[MAX_SIGNALS];  // Declare the array as extern
extern int currentSignalIndex;

#endif // FLASHINGSIGNALS_H
